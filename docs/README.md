# docs/ — เอกสารภายใน (อยู่ในเครื่องเท่านั้น ไม่ push ขึ้น GitHub)

> ⚠️ **โฟลเดอร์นี้ถูก gitignore ไว้ทั้งโฟลเดอร์ — ตั้งใจ ห้ามเอาออกจาก .gitignore**
>
> เหตุผล: repo `24logisticsjobslip` เป็น **public** และ GitHub Pages เสิร์ฟทุกไฟล์บน main เป็นเว็บสาธารณะ
> เอกสารในนี้มีข้อมูลภายใน (แผนธุรกิจ · ปริมาณงาน · จำนวนคนขับ · แผนระบบ)
> ถ้า commit = คนนอกเปิดอ่านได้ที่ `ohmkanatip.github.io/24logisticsjobslip/docs/...`
> — บทเรียนเดียวกับ V63.6 ที่ต้องถอดชื่อคนขับ/ที่อยู่ลูกค้าออกจาก repo มาแล้ว

| ไฟล์ | คืออะไร |
|---|---|
| `24LOGISTICS-MASTER-SPEC.md` | แผนแม่บท งาน ①–⑥ + สเปกงาน ① (เชื่อมจ่ายงาน→ใบงานร่าง) |
| `PROMPT-OCR-เบอร์ตู้.md` | พร้อมพ์งาน ③ — OCR อ่านเบอร์ตู้ผ่าน LINE |
| `PROMPT-แผนที่-GPS-scaffold.md` | พร้อมพ์งาน ② — แผนที่ติดตามรถ + GPS บน Cloudflare D1 |
| `SETUP-โครงโปรเจกต์-VSCode.md` | วิธีวางโครง monorepo + multi-root workspace (ไฟล์นี้เป็นต้นทางของโครง `apps/`) |

**สำเนาต้นฉบับ:** `~/Downloads/` (ที่เจ้าของอัปโหลด) และ `เอกสารและไฟล์เก่า/` — โฟลเดอร์นี้เป็นสำเนาไว้ให้ VS Code workspace เปิดอ่านสะดวก
