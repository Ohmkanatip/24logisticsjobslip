# ตัวอย่างไฟล์ตัวแปรลับสำหรับรันในเครื่อง — ก๊อปเป็น .dev.vars แล้วค่อยเติมค่าจริง
# ⚠️ ห้าม commit ค่าจริงเด็ดขาด (ไฟล์นี้มีแต่ค่าว่าง = ปลอดภัย)

# provider แผนที่: leaflet (ไม่ต้องมี key) / longdo / google / mapbox
MAP_PROVIDER=leaflet

# key ฝั่ง client (ฝังในหน้าเว็บได้ เช่น key แสดงแผนที่ที่จำกัดโดเมนแล้ว)
MAP_API_KEY=

# key ฝั่ง server (geocode / distance matrix) — ⚠️ ห้ามหลุดไป client เด็ดขาด ใช้ใน Worker เท่านั้น
MAP_SERVER_KEY=

# ที่อยู่ API ของ Cartrack (ยังไม่มี — ปล่อยว่างไว้ ระบบจะเข้าโหมด mock เอง)
CARTRACK_API_URL=

# API key ของ Cartrack — ไม่มีค่า = โหมด mock ทั้งระบบ (mock feed + memory repo)
CARTRACK_API_KEY=

# ชื่อ R2 bucket สำหรับสำรอง gps_track ที่เก่ากว่า 60 วัน
R2_BUCKET=

# โทเคนกันคนนอกยิงปิงปลอมเข้า /api/fleet/ingest — ก่อนใช้จริงต้องตั้งเสมอ (ว่าง = โหมดทดลองในเครื่อง ไม่ตรวจ)
INGEST_TOKEN=
