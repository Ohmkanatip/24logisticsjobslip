# CLAUDE.md — OCR อ่านเบอร์ตู้คอนเทนเนอร์ผ่าน LINE (`apps/ocr`)

## โปรเจกต์นี้คืออะไร
บอท LINE สำหรับคนขับ ~46 คน: ถ่ายรูปตู้ส่งแชท 1:1 → บอทอ่านเบอร์ตู้ (OCR) → ตรวจเช็คดิจิตตามมาตรฐาน **ISO 6346** → คนขับกดปุ่มยืนยัน → ระบบเติมเบอร์ตู้เข้าใบงานร่างให้

**สถานะ: โครงรอ (mock-first) — ห้าม deploy** · ทุกอย่างรันได้ทันทีโดยไม่มี key จริง
- OCR engine จริงยังไม่เคาะ → ใช้ mock (`OCR_PROVIDER=mock`)
- **การเขียนกลับ: เคาะแล้ว (28 ส.ค.) ทาง ③ d1 staging — implement จริงแล้ว** (`src/db/staging.js` + endpoint `GET /api/ocr/results` / `POST /api/ocr/pulled` + ด่าน `PULL_TOKEN`) · บอทไม่แตะชีท เว็บดึงเอง — ดู `ARCHITECTURE-OPTIONS.md`

## ⚠️ กติกาเหล็ก (ห้ามฝ่าฝืน)
- **เช็คดิจิตไม่ผ่าน = flag เท่านั้น — ห้ามเติมเข้าใบงานเอง** ระบบเสนอทางซ่อม (`suggestRepairs`) ให้คนเลือกได้ แต่ห้าม auto-ใช้
- **คนขับต้องกดยืนยันเสมอ — ไม่เขียนทับใบงานเงียบๆ** ทุกการเขียนกลับต้องมาจาก postback ยืนยัน และตรวจเช็คดิจิตซ้ำก่อนเขียนทุกครั้ง
- ห้ามมี secret/API key ในโค้ดหรือไฟล์ config — ตั้งผ่าน `wrangler secret put` เท่านั้น (placeholder ว่างอยู่ใน `.dev.vars.example`)
- stub ต้องซื่อสัตย์: ของที่ยังไม่ implement ตอบ `{ ok:false, reason:'not-implemented' }` — ห้ามแกล้งสำเร็จ (ยกเว้น mock ที่ติดป้าย `mock:true` ชัดๆ)
- logic ห้ามเรียก `Date.now()`/`new Date()` เอง — รับ timestamp เป็นพารามิเตอร์ (ให้ชั้นนอกส่งเวลาจริงเข้ามา)
- หมายเหตุกติกาโปรเจกต์แม่: V63.8 เลิกใช้ LINE "แจ้งเตือน" แล้ว — บอทนี้คือ "รับรูปจากคนขับ" คนละหน้าที่ ไม่ขัดกติกานั้น

## แผน 2 ระดับ
1. **① LINE chat bot MVP** — คนขับส่งรูปในแชท บอทตอบเบอร์+ผลตรวจ+ปุ่มยืนยัน · ยังไม่รู้ว่ารูปคู่กับใบงานไหน (ออฟฟิศ/คนขับเลือกทีหลัง หรือจับคู่จากทะเบียนรถ+เวลา)
2. **② LIFF** — คนขับเปิดหน้าเว็บใน LINE เลือกงานของตัวเองก่อนถ่าย → รูปผูกใบงานถูกใบอัตโนมัติ (`jobUid` ติดไปกับ postback ตั้งแต่ต้น)

## เช็คดิจิต ISO 6346 (implement จริงแล้วใน `src/iso6346/check.js`)
- ตัวอักษร 4 + เลข 6 + เช็คดิจิต 1 · ค่าตัวอักษร A=10…Z=38 **ข้ามพหุคูณของ 11** (ไม่มี 11/22/33) · ตำแหน่ง i=0..9 คูณ 2^i → รวม → mod 11 → ได้ 10 ใช้ 0
- เวกเตอร์ทดสอบบังคับ: **CSQU3054383 → เช็คดิจิต 3 (ผ่าน)** — ล็อกไว้ในเทสแล้ว

## ตารางเทียบ OCR engine (รอเคาะ)
> ⚠️ **ตัวเลขต้นทุนเป็นค่าประมาณ ต้องเช็คราคาปัจจุบันก่อนเคาะ** · ที่ปริมาณงานจริง ~60-100 ตู้/วัน (~3,000 รูป/เดือน) ทุกเจ้าหลักอยู่แค่ **หลักร้อยบาท/เดือน** — ต้นทุนไม่ใช่ตัวตัดสิน ความแม่นกับความง่ายสำคัญกว่า

| Engine | จุดแข็ง | จุดอ่อน | ต้นทุนโดยประมาณ/1,000 รูป |
|---|---|---|---|
| **Qwen-VL** (Alibaba / OpenRouter) | โมเดล vision อ่านตัวเลขบนวัตถุจริงเก่ง สั่ง prompt ให้ตอบเฉพาะเบอร์ได้ ราคาถูกมาก | เสี่ยง hallucinate แต่งเลขเอง — ต้องพึ่งเช็คดิจิตกรองเสมอ | ~7-40 บาท |
| **Google Cloud Vision** | OCR เสถียร บริการนิ่ง เอกสารดี | คืนข้อความทั้งภาพ ต้อง extract เอง · ตั้ง billing ยุ่ง | ~55 บาท (ฟรี 1,000 แรก/เดือน) |
| **Typhoon OCR** (SCB 10X — ไทย) | ทีมไทย ซัพพอร์ตไทย · open weights self-host ได้ | เกิดมาเพื่อเอกสาร — ต้องลองกับรูปตู้จริงก่อน | หลักสิบ-ร้อยบาท (API) |
| **Self-host** (Typhoon/PaddleOCR บน GPU) | ข้อมูลไม่ออกนอกบ้าน ไม่มีค่า per-call | ต้องดูแลเครื่องเอง · ที่ปริมาณนี้ไม่คุ้ม | ค่าเครื่อง ~หลักร้อย-พันบาท/เดือน |

## โครงไฟล์
- `src/iso6346/` — `check.js` (normalize/format/เช็คดิจิต/validate) · `repair.js` (ข้อเสนอซ่อมจากคู่ตัวอักษรที่ OCR สับสน)
- `src/ocr/` — `engine.js` (เลือกตาม `OCR_PROVIDER`) · `extract.js` (หา pattern เบอร์จากข้อความ) · `engines/` (mock ใช้ได้จริง · qwen/vision/typhoon เป็น stub ซื่อสัตย์)
- `src/line/` — `signature.js` (HMAC-SHA256 ตรวจ webhook — ของจริง) · `client.js` (mock + stub จริง) · `webhook.js` (flow หลัก ฉีด deps ได้)
- `src/writeback/` — `index.js` (interface `fillContainer` + mock) · adapter 3 แบบรอเคาะ
- `src/worker/index.js` — `POST /webhook/line` (ตรวจลายเซ็นก่อนเสมอ · ไม่มี secret = โหมด mock) · `GET /health`

## วิธีรันเทส
```bash
cd "apps/ocr" && node tests/run.js   # ไม่มี npm dependency ใดๆ · ผ่าน = exit 0 · สรุปท้าย "ผ่าน X · ตก Y"
```

## สิ่งที่รอเคาะ (อย่าทำก่อนเจ้าของสั่ง)
1. เลือก OCR engine (ตารางด้านบน — ควรลองยิงรูปตู้จริง ~30 รูปเทียบความแม่นก่อน)
2. ~~วิธีเขียนกลับใบงาน~~ ✅ เคาะแล้ว: ทาง ③ d1 staging (implement แล้ว) — **ยังเหลือฝั่งเว็บ jobslip: ปุ่ม/จุดดึงผลมาเติมช่องเบอร์ตู้** (แตะ production ต้องขอเจ้าของก่อนตามกติกา)
3. สร้าง LINE Official Account + Messaging API channel ใหม่ (ของเดิมเลิกใช้ไปแล้ว)
4. วิธีจับคู่รูป↔ใบงานในเฟส ① (ก่อนมี LIFF)
5. deploy — ห้ามจนกว่าข้อ 1-3 จะเคาะครบ
